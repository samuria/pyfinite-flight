# Core Flask and WebSocket support
flask==3.0.0
flask-socketio==5.3.6
python-socketio==5.11.0

# CORS support for development
flask-cors==4.0.0

# Python standard library only - no external dependencies for the client itself